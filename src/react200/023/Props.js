import React from "react";
import datatype from "prop-types";

const PropsNode = ({children}) => {
    return (<div style={{padding: "0px"}}>
        {children}
    </div>)
}

export default PropsNode;
